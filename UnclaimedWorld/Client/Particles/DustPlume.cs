using System;
using System.Collections.Generic;
using System.Text;
using Microsoft.Xna.Framework;
namespace UWGame.ClientSide.Particles
{
    /*
    public class DustPlume
    {
        public Vector2 Position;

        /// <summary>
        /// 0 - 1
        /// </summary>
        public float Intensity;

        // keep a timer that will tell us when it's time to add more particles to the
        // dust plume.
       
        public const float TimeBetweenDustPuffs = .6f;
        public float timeTillDustPuff = 0.0f;


        public void Initialize()
        {
            timeTillDustPuff = 0.0f;

        }

        public void Update(float elapsed)
        {
            timeTillDustPuff -= elapsed;


        }

        /// <summary>
        /// if more than x seconds since last dust puff => not active.
        /// </summary>
        /// <returns></returns>
        public bool IsActive()
        {
            if (timeTillDustPuff < -5f)
            {
                return false;
            }
            return true;

        }
    }*/
}
